// Quiz wizard — implementado en Tarea 4
export default function QuizPage() {
  return (
    <main
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: 'var(--bg-primary)',
        color: 'var(--text-muted)',
        fontFamily: 'var(--font-cormorant), serif',
        fontSize: '1.25rem',
      }}
    >
      Quiz — Próximamente
    </main>
  )
}
